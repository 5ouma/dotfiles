name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Slim
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@f0d974bfa84423faffd60e4337c744fc89ade698 # v0.7.1
        with:
          config: data/prek.yml
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          SKIP: op-launcher
